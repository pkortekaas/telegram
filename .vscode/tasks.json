{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "command": "${config.python.pythonPath}",
    "args": [
        "${file}"
    ],
    "tasks": [
        {
            "label": "${config.python.pythonPath}",
            "type": "shell",
            "command": "${config.python.pythonPath}",
            "args": [
                "${file}"
            ],
            "problemMatcher": [],
            "group": "build"
        }
    ]
}